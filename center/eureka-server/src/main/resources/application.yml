spring:
  application:
    name: eurekaserver
server:
  port: 8000
eureka:
  client:
#   注册自身
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://eureka:8000/eureka